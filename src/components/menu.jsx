import {Link} from "gatsby";
import PropTypes from "prop-types";
import React from "react";

const Menu = ({links}) => (
    <div></div>
);

Menu.propTypes = {
    links: PropTypes.array
};

Menu.defaultProps = {
};

export default Menu;
