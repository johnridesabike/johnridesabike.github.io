{% macro latestPosts(tag) %}
<ul class="wp-block-latest-posts">
{% for page in tag %}
    {% if loop.index <= 3 %}
    <li><a href="{{ page.url }}">{{ page.data.title }}</a></li>
    {% endif %}
{% endfor %}
</ul>
{% endmacro %}